# Copier ce fichier en .env.local puis modifier selon le besoin.
# .env.local n'est pas versionné (gitignore).

# Mode test API : forcer l'utilisateur pour GET /api/leads
# Décommenter pour activer le mode test :
APP_TEST_USER=ebraux

# Mode normal : ne rien définir ou commenter la ligne ci-dessus.
